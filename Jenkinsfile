pipeline {
    agent any
    stages {
        stage('git clone') {
            steps {                
               sh 'git url: 'https://github.com/ramrpk/maven-project1.git', branch: 'master''
            }
        }
    }  
}
